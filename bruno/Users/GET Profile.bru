meta {
  name: GET Profile
  type: http
  seq: 2
}

get {
  url: {{base_url}}/profile
  body: json
  auth: inherit
}

body:json {
  {
    "username":"admin",
    "password":"password"
  }
}

settings {
  encodeUrl: true
}
